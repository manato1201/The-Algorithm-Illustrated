---
name: パーリンノイズ
category: ゲーム
subcategory: 手続き型コンテンツ生成
complexity: O(1)(1点あたり、次元数に依存)
summary: 格子点に割り当てた擬似乱数の勾配を補間して滑らかな連続ノイズを生成する。地形・雲・テクスチャ生成の定番。
---

## 概要

単純に各点へ独立な乱数を割り当てると、まだら模様のホワイトノイズになり、自然の地形や雲のような「なめらかに変化するランダムさ」は表現できない。1983年にKen Perlinが考案したパーリンノイズは、格子点ごとに固定された擬似乱数の「勾配ベクトル」を割り当て、その格子点間を滑らかに補間することで、連続的でありながら決定論的な(同じ入力なら常に同じ出力になる)自然な起伏を生成する。この功績によりPerlinはアカデミー賞(技術部門)を受賞しており、以来、地形生成・雲や炎のテクスチャ・キャラクターの揺らぎのアニメーションなど、CG・ゲーム開発のあらゆる場面で使われ続けている。

## 仕組み

1. 空間を格子(グリッド)に分割し、各格子点にハッシュ関数などで決定論的な擬似乱数の勾配ベクトルを割り当てる(同じ格子点には常に同じベクトルが対応する)
2. ノイズ値を求めたい点`P`が属する格子セルの4つ(2次元の場合)の格子点それぞれについて、「格子点からPへ向かうベクトル」と「その格子点の勾配ベクトル」の内積を計算する(これがその格子点からの寄与度になる)
3. 4つの内積値を、`P`のセル内での位置に応じて滑らかな補間関数(**S字カーブ**、急激な変化を避けるためのイーズ関数)を使って補間し、最終的なノイズ値を得る
4. 周波数(格子の細かさ)と振幅を変えながら複数回ノイズを重ね合わせる(**オクターブ**)ことで、大きなうねりに細かいディテールを重ねたフラクタルなノイズ(fBm)を作るのが実用上の定石

## 特性・トレードオフ

- **決定論的で再現可能**: 同じ座標に対して常に同じ値を返すため、シード値さえ共有すれば広大な地形を保存せずにいつでも同じ形で再生成できる(Minecraftの地形生成などが代表例)
- **連続性**: 隣接する点の値が滑らかに変化するため、地形の高さマップや雲のテクスチャのように「自然な連続性」が求められる用途に適する。ホワイトノイズや単純な乱数格子ではこの性質が得られない
- **計算コストと品質のトレードオフ**: オクターブ数を増やすほどディテールが豊かになるが計算コストも増える。リアルタイム生成が必要なゲームでは必要なオクターブ数を絞る調整が重要になる
- **改良版の存在**: 元祖パーリンノイズには方向性のアーティファクト(格子軸に沿った規則性が見える)があり、Perlin自身が改良した**シンプレックスノイズ**などの派生アルゴリズムが計算効率・品質の面でしばしば好まれる

## 実装例

Ken Perlinのリファレンス実装で使われる順列テーブル(全言語で共通の定数)を使い、2次元のパーリンノイズを計算する。格子点ちょうどの位置では勾配ベクトルとの内積を取る距離ベクトルがゼロになるため、値が厳密に0になることを検証する。

```python
import math

# Ken Perlinのリファレンス実装で使われる順列テーブル(公開されている定数、全言語で共通)
_PERM_BASE = [
    151, 160, 137, 91, 90, 15, 131, 13, 201, 95, 96, 53, 194, 233, 7, 225,
    140, 36, 103, 30, 69, 142, 8, 99, 37, 240, 21, 10, 23, 190, 6, 148,
    247, 120, 234, 75, 0, 26, 197, 62, 94, 252, 219, 203, 117, 35, 11, 32,
    57, 177, 33, 88, 237, 149, 56, 87, 174, 20, 125, 136, 171, 168, 68, 175,
    74, 165, 71, 134, 139, 48, 27, 166, 77, 146, 158, 231, 83, 111, 229, 122,
    60, 211, 133, 230, 220, 105, 92, 41, 55, 46, 245, 40, 244, 102, 143, 54,
    65, 25, 63, 161, 1, 216, 80, 73, 209, 76, 132, 187, 208, 89, 18, 169,
    200, 196, 135, 130, 116, 188, 159, 86, 164, 100, 109, 198, 173, 186, 3, 64,
    52, 217, 226, 250, 124, 123, 5, 202, 38, 147, 118, 126, 255, 82, 85, 212,
    207, 206, 59, 227, 47, 16, 58, 17, 182, 189, 28, 42, 223, 183, 170, 213,
    119, 248, 152, 2, 44, 154, 163, 70, 221, 153, 101, 155, 167, 43, 172, 9,
    129, 22, 39, 253, 19, 98, 108, 110, 79, 113, 224, 232, 178, 185, 112, 104,
    218, 246, 97, 228, 251, 34, 242, 193, 238, 210, 144, 12, 191, 179, 162, 241,
    81, 51, 145, 235, 249, 14, 239, 107, 49, 192, 214, 31, 181, 199, 106, 157,
    184, 84, 204, 176, 115, 121, 50, 45, 127, 4, 150, 254, 138, 236, 205, 93,
    222, 114, 67, 29, 24, 72, 243, 141, 128, 195, 78, 66, 215, 61, 156, 180,
]
PERM = _PERM_BASE * 2  # 境界での配列外参照を避けるため2つ連結しておく


def _fade(t: float) -> float:
    # 5次のS字カーブ: 補間の両端で1階・2階微分がゼロになり継ぎ目が滑らかになる
    return t * t * t * (t * (t * 6 - 15) + 10)


def _lerp(t: float, a: float, b: float) -> float:
    return a + t * (b - a)


def _grad2(hash_val: int, x: float, y: float) -> float:
    h = hash_val & 3
    u = x if h < 2 else y
    v = y if h < 2 else x
    return (u if (h & 1) == 0 else -u) + (v if (h & 2) == 0 else -v)


def perlin2(x: float, y: float) -> float:
    xi, yi = math.floor(x) & 255, math.floor(y) & 255
    xf, yf = x - math.floor(x), y - math.floor(y)
    u, v = _fade(xf), _fade(yf)

    aa = PERM[PERM[xi] + yi]
    ab = PERM[PERM[xi] + yi + 1]
    ba = PERM[PERM[xi + 1] + yi]
    bb = PERM[PERM[xi + 1] + yi + 1]

    x1 = _lerp(u, _grad2(aa, xf, yf), _grad2(ba, xf - 1, yf))
    x2 = _lerp(u, _grad2(ab, xf, yf - 1), _grad2(bb, xf - 1, yf - 1))
    return _lerp(v, x1, x2)
```

```typescript
// Ken Perlinのリファレンス実装で使われる順列テーブル(公開されている定数、全言語で共通)
const PERM_BASE = [
  151, 160, 137, 91, 90, 15, 131, 13, 201, 95, 96, 53, 194, 233, 7, 225,
  140, 36, 103, 30, 69, 142, 8, 99, 37, 240, 21, 10, 23, 190, 6, 148,
  247, 120, 234, 75, 0, 26, 197, 62, 94, 252, 219, 203, 117, 35, 11, 32,
  57, 177, 33, 88, 237, 149, 56, 87, 174, 20, 125, 136, 171, 168, 68, 175,
  74, 165, 71, 134, 139, 48, 27, 166, 77, 146, 158, 231, 83, 111, 229, 122,
  60, 211, 133, 230, 220, 105, 92, 41, 55, 46, 245, 40, 244, 102, 143, 54,
  65, 25, 63, 161, 1, 216, 80, 73, 209, 76, 132, 187, 208, 89, 18, 169,
  200, 196, 135, 130, 116, 188, 159, 86, 164, 100, 109, 198, 173, 186, 3, 64,
  52, 217, 226, 250, 124, 123, 5, 202, 38, 147, 118, 126, 255, 82, 85, 212,
  207, 206, 59, 227, 47, 16, 58, 17, 182, 189, 28, 42, 223, 183, 170, 213,
  119, 248, 152, 2, 44, 154, 163, 70, 221, 153, 101, 155, 167, 43, 172, 9,
  129, 22, 39, 253, 19, 98, 108, 110, 79, 113, 224, 232, 178, 185, 112, 104,
  218, 246, 97, 228, 251, 34, 242, 193, 238, 210, 144, 12, 191, 179, 162, 241,
  81, 51, 145, 235, 249, 14, 239, 107, 49, 192, 214, 31, 181, 199, 106, 157,
  184, 84, 204, 176, 115, 121, 50, 45, 127, 4, 150, 254, 138, 236, 205, 93,
  222, 114, 67, 29, 24, 72, 243, 141, 128, 195, 78, 66, 215, 61, 156, 180,
];
const PERM = [...PERM_BASE, ...PERM_BASE]; // 境界での配列外参照を避けるため2つ連結しておく

function fade(t: number): number {
  // 5次のS字カーブ: 補間の両端で1階・2階微分がゼロになり継ぎ目が滑らかになる
  return t * t * t * (t * (t * 6 - 15) + 10);
}

function lerp(t: number, a: number, b: number): number {
  return a + t * (b - a);
}

function grad2(hashVal: number, x: number, y: number): number {
  const h = hashVal & 3;
  const u = h < 2 ? x : y;
  const v = h < 2 ? y : x;
  return ((h & 1) === 0 ? u : -u) + ((h & 2) === 0 ? v : -v);
}

function perlin2(x: number, y: number): number {
  const xi = Math.floor(x) & 255;
  const yi = Math.floor(y) & 255;
  const xf = x - Math.floor(x);
  const yf = y - Math.floor(y);
  const u = fade(xf);
  const v = fade(yf);

  const aa = PERM[PERM[xi] + yi];
  const ab = PERM[PERM[xi] + yi + 1];
  const ba = PERM[PERM[xi + 1] + yi];
  const bb = PERM[PERM[xi + 1] + yi + 1];

  const x1 = lerp(u, grad2(aa, xf, yf), grad2(ba, xf - 1, yf));
  const x2 = lerp(u, grad2(ab, xf, yf - 1), grad2(bb, xf - 1, yf - 1));
  return lerp(v, x1, x2);
}
```

```cpp
#include <array>
#include <cmath>

// Ken Perlinのリファレンス実装で使われる順列テーブル(公開されている定数、全言語で共通)
constexpr int kPermBase[256] = {
    151, 160, 137, 91, 90, 15, 131, 13, 201, 95, 96, 53, 194, 233, 7, 225,
    140, 36, 103, 30, 69, 142, 8, 99, 37, 240, 21, 10, 23, 190, 6, 148,
    247, 120, 234, 75, 0, 26, 197, 62, 94, 252, 219, 203, 117, 35, 11, 32,
    57, 177, 33, 88, 237, 149, 56, 87, 174, 20, 125, 136, 171, 168, 68, 175,
    74, 165, 71, 134, 139, 48, 27, 166, 77, 146, 158, 231, 83, 111, 229, 122,
    60, 211, 133, 230, 220, 105, 92, 41, 55, 46, 245, 40, 244, 102, 143, 54,
    65, 25, 63, 161, 1, 216, 80, 73, 209, 76, 132, 187, 208, 89, 18, 169,
    200, 196, 135, 130, 116, 188, 159, 86, 164, 100, 109, 198, 173, 186, 3, 64,
    52, 217, 226, 250, 124, 123, 5, 202, 38, 147, 118, 126, 255, 82, 85, 212,
    207, 206, 59, 227, 47, 16, 58, 17, 182, 189, 28, 42, 223, 183, 170, 213,
    119, 248, 152, 2, 44, 154, 163, 70, 221, 153, 101, 155, 167, 43, 172, 9,
    129, 22, 39, 253, 19, 98, 108, 110, 79, 113, 224, 232, 178, 185, 112, 104,
    218, 246, 97, 228, 251, 34, 242, 193, 238, 210, 144, 12, 191, 179, 162, 241,
    81, 51, 145, 235, 249, 14, 239, 107, 49, 192, 214, 31, 181, 199, 106, 157,
    184, 84, 204, 176, 115, 121, 50, 45, 127, 4, 150, 254, 138, 236, 205, 93,
    222, 114, 67, 29, 24, 72, 243, 141, 128, 195, 78, 66, 215, 61, 156, 180,
};

int perm(int i) {
    // 境界での配列外参照を避けるため、範囲外は256の剰余で折り返す(2つ連結したのと等価)
    return kPermBase[i & 255];
}

double fade(double t) {
    // 5次のS字カーブ: 補間の両端で1階・2階微分がゼロになり継ぎ目が滑らかになる
    return t * t * t * (t * (t * 6 - 15) + 10);
}

double lerp(double t, double a, double b) {
    return a + t * (b - a);
}

double grad2(int hashVal, double x, double y) {
    int h = hashVal & 3;
    double u = h < 2 ? x : y;
    double v = h < 2 ? y : x;
    return ((h & 1) == 0 ? u : -u) + ((h & 2) == 0 ? v : -v);
}

double perlin2(double x, double y) {
    int xi = static_cast<int>(std::floor(x)) & 255;
    int yi = static_cast<int>(std::floor(y)) & 255;
    double xf = x - std::floor(x);
    double yf = y - std::floor(y);
    double u = fade(xf);
    double v = fade(yf);

    int aa = perm(perm(xi) + yi);
    int ab = perm(perm(xi) + yi + 1);
    int ba = perm(perm(xi + 1) + yi);
    int bb = perm(perm(xi + 1) + yi + 1);

    double x1 = lerp(u, grad2(aa, xf, yf), grad2(ba, xf - 1, yf));
    double x2 = lerp(u, grad2(ab, xf, yf - 1), grad2(bb, xf - 1, yf - 1));
    return lerp(v, x1, x2);
}
```

```rust
// Ken Perlinのリファレンス実装で使われる順列テーブル(公開されている定数、全言語で共通)
const PERM_BASE: [i32; 256] = [
    151, 160, 137, 91, 90, 15, 131, 13, 201, 95, 96, 53, 194, 233, 7, 225,
    140, 36, 103, 30, 69, 142, 8, 99, 37, 240, 21, 10, 23, 190, 6, 148,
    247, 120, 234, 75, 0, 26, 197, 62, 94, 252, 219, 203, 117, 35, 11, 32,
    57, 177, 33, 88, 237, 149, 56, 87, 174, 20, 125, 136, 171, 168, 68, 175,
    74, 165, 71, 134, 139, 48, 27, 166, 77, 146, 158, 231, 83, 111, 229, 122,
    60, 211, 133, 230, 220, 105, 92, 41, 55, 46, 245, 40, 244, 102, 143, 54,
    65, 25, 63, 161, 1, 216, 80, 73, 209, 76, 132, 187, 208, 89, 18, 169,
    200, 196, 135, 130, 116, 188, 159, 86, 164, 100, 109, 198, 173, 186, 3, 64,
    52, 217, 226, 250, 124, 123, 5, 202, 38, 147, 118, 126, 255, 82, 85, 212,
    207, 206, 59, 227, 47, 16, 58, 17, 182, 189, 28, 42, 223, 183, 170, 213,
    119, 248, 152, 2, 44, 154, 163, 70, 221, 153, 101, 155, 167, 43, 172, 9,
    129, 22, 39, 253, 19, 98, 108, 110, 79, 113, 224, 232, 178, 185, 112, 104,
    218, 246, 97, 228, 251, 34, 242, 193, 238, 210, 144, 12, 191, 179, 162, 241,
    81, 51, 145, 235, 249, 14, 239, 107, 49, 192, 214, 31, 181, 199, 106, 157,
    184, 84, 204, 176, 115, 121, 50, 45, 127, 4, 150, 254, 138, 236, 205, 93,
    222, 114, 67, 29, 24, 72, 243, 141, 128, 195, 78, 66, 215, 61, 156, 180,
];

fn perm(i: i32) -> i32 {
    // 境界での配列外参照を避けるため、範囲外は256の剰余で折り返す(2つ連結したのと等価)
    PERM_BASE[(i & 255) as usize]
}

fn fade(t: f64) -> f64 {
    // 5次のS字カーブ: 補間の両端で1階・2階微分がゼロになり継ぎ目が滑らかになる
    t * t * t * (t * (t * 6.0 - 15.0) + 10.0)
}

fn lerp(t: f64, a: f64, b: f64) -> f64 {
    a + t * (b - a)
}

fn grad2(hash_val: i32, x: f64, y: f64) -> f64 {
    let h = hash_val & 3;
    let u = if h < 2 { x } else { y };
    let v = if h < 2 { y } else { x };
    (if h & 1 == 0 { u } else { -u }) + (if h & 2 == 0 { v } else { -v })
}

fn perlin2(x: f64, y: f64) -> f64 {
    let xi = x.floor() as i32 & 255;
    let yi = y.floor() as i32 & 255;
    let xf = x - x.floor();
    let yf = y - y.floor();
    let u = fade(xf);
    let v = fade(yf);

    let aa = perm(perm(xi) + yi);
    let ab = perm(perm(xi) + yi + 1);
    let ba = perm(perm(xi + 1) + yi);
    let bb = perm(perm(xi + 1) + yi + 1);

    let x1 = lerp(u, grad2(aa, xf, yf), grad2(ba, xf - 1.0, yf));
    let x2 = lerp(u, grad2(ab, xf, yf - 1.0), grad2(bb, xf - 1.0, yf - 1.0));
    lerp(v, x1, x2)
}
```

```csharp
// Ken Perlinのリファレンス実装で使われる順列テーブル(公開されている定数、全言語で共通)
static readonly int[] PermBase = {
    151, 160, 137, 91, 90, 15, 131, 13, 201, 95, 96, 53, 194, 233, 7, 225,
    140, 36, 103, 30, 69, 142, 8, 99, 37, 240, 21, 10, 23, 190, 6, 148,
    247, 120, 234, 75, 0, 26, 197, 62, 94, 252, 219, 203, 117, 35, 11, 32,
    57, 177, 33, 88, 237, 149, 56, 87, 174, 20, 125, 136, 171, 168, 68, 175,
    74, 165, 71, 134, 139, 48, 27, 166, 77, 146, 158, 231, 83, 111, 229, 122,
    60, 211, 133, 230, 220, 105, 92, 41, 55, 46, 245, 40, 244, 102, 143, 54,
    65, 25, 63, 161, 1, 216, 80, 73, 209, 76, 132, 187, 208, 89, 18, 169,
    200, 196, 135, 130, 116, 188, 159, 86, 164, 100, 109, 198, 173, 186, 3, 64,
    52, 217, 226, 250, 124, 123, 5, 202, 38, 147, 118, 126, 255, 82, 85, 212,
    207, 206, 59, 227, 47, 16, 58, 17, 182, 189, 28, 42, 223, 183, 170, 213,
    119, 248, 152, 2, 44, 154, 163, 70, 221, 153, 101, 155, 167, 43, 172, 9,
    129, 22, 39, 253, 19, 98, 108, 110, 79, 113, 224, 232, 178, 185, 112, 104,
    218, 246, 97, 228, 251, 34, 242, 193, 238, 210, 144, 12, 191, 179, 162, 241,
    81, 51, 145, 235, 249, 14, 239, 107, 49, 192, 214, 31, 181, 199, 106, 157,
    184, 84, 204, 176, 115, 121, 50, 45, 127, 4, 150, 254, 138, 236, 205, 93,
    222, 114, 67, 29, 24, 72, 243, 141, 128, 195, 78, 66, 215, 61, 156, 180,
};
static readonly int[] Perm = PermBase.Concat(PermBase).ToArray(); // 境界での配列外参照を避けるため2つ連結しておく

static double Fade(double t) =>
    // 5次のS字カーブ: 補間の両端で1階・2階微分がゼロになり継ぎ目が滑らかになる
    t * t * t * (t * (t * 6 - 15) + 10);

static double Lerp(double t, double a, double b) => a + t * (b - a);

static double Grad2(int hashVal, double x, double y)
{
    int h = hashVal & 3;
    double u = h < 2 ? x : y;
    double v = h < 2 ? y : x;
    return ((h & 1) == 0 ? u : -u) + ((h & 2) == 0 ? v : -v);
}

static double Perlin2(double x, double y)
{
    int xi = (int)Math.Floor(x) & 255;
    int yi = (int)Math.Floor(y) & 255;
    double xf = x - Math.Floor(x);
    double yf = y - Math.Floor(y);
    double u = Fade(xf);
    double v = Fade(yf);

    int aa = Perm[Perm[xi] + yi];
    int ab = Perm[Perm[xi] + yi + 1];
    int ba = Perm[Perm[xi + 1] + yi];
    int bb = Perm[Perm[xi + 1] + yi + 1];

    double x1 = Lerp(u, Grad2(aa, xf, yf), Grad2(ba, xf - 1, yf));
    double x2 = Lerp(u, Grad2(ab, xf, yf - 1), Grad2(bb, xf - 1, yf - 1));
    return Lerp(v, x1, x2);
}
```
